include wlstringfmt // Include the WLSTD file.

namespace filesystem:

  struct save_template:
    
  end

  function save(file: wfile, content: vector<string>, st: save_template)(void):
    
  end
end
